// This spec is intentionally skipped. Replace the placeholder seed and
// update the test when you have real end-to-end coverage.
describe("example placeholder", () => {
  it("bootstraps a fresh database", () => {
    cy.reSeedDb();
  });
});
